pipeline {
    agent {
        node {
            label 'docker' && 'maven'
        }
    }
    stages { 	
        stage('Build test') {
            steps {
                echo 'Hello world'
            }
        }
        
}
